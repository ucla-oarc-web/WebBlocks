<h2>Structure Variables</h2>

<div class="message warning"><strong>WARNING</strong> This page is currently a stub. It includes a list of all related variables, but it will later be extended to include descriptions of how exactly these variables may be used.</div>

<ul>
    <li><%= DOC.vars.sass.render('structure-container-gutter') %></li>
    <li><%= DOC.vars.sass.render('structure-container-width-max') %></li>
</ul>

<ul>
    <li><%= DOC.vars.sass.render('structure-panels') %></li>
    <li><%= DOC.vars.sass.render('structure-panel-gutter') %></li>
</ul>